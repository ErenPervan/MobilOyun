<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    tools:context=".OyunActivity">

    <!-- Ana LinearLayout - Tüm oyun elemanlarını içerir -->
    <LinearLayout
        android:id="@+id/lyn"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Üst Bilgi Paneli - Oyuncu ismi, skor ve zorluk seviyesi -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp"
            android:background="#FFFFFF"
            android:elevation="4dp"
            android:layout_marginBottom="12dp">

            <!-- Birinci satır: İsim ve Skor -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/bilgiTxt"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Hoş Geldiniz"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="#333333" />

                <TextView
                    android:id="@+id/skorTxt"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🏆 Skor: 0"
                    android:textSize="18sp"
                    android:textColor="#4CAF50"
                    android:textStyle="bold"
                    android:layout_marginStart="8dp" />
            </LinearLayout>

            <!-- İkinci satır: Hata Hakkı -->
            <TextView
                android:id="@+id/hataHakkiTxt"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="❤️ Hak: 10"
                android:textSize="14sp"
                android:textColor="#E53935"
                android:textStyle="bold"
                android:layout_marginTop="4dp" />
        </LinearLayout>

        <!-- Timer ve Progress Bar Paneli -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp"
            android:background="#FFFFFF"
            android:elevation="4dp"
            android:layout_marginBottom="12dp">

            <!-- Süre Göstergesi TextView -->
            <TextView
                android:id="@+id/sureTxt"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="Süre: 0sn"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#1976D2"
                android:layout_marginBottom="8dp" />

            <!-- Progress Bar (Süre görselleştirmesi) -->
            <ProgressBar
                android:id="@+id/sureBar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="12dp"
                android:max="100"
                android:progress="100"
                android:progressTint="#4CAF50"
                android:progressBackgroundTint="#E0E0E0" />
        </LinearLayout>

        <!-- ScrollView - Grid taşarsa kaydırma özelliği sağlar -->
        <!-- Kart sayısı çok olduğunda (örn: Zor seviye) içerik scroll edilebilir -->
        <ScrollView
            android:id="@+id/scrollView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:fillViewport="true"
            android:scrollbars="vertical">

            <!-- Oyun Grid'i - Kartların yerleştirileceği alan -->
            <!-- ScrollView içinde olduğu için artık taşma sorunu olmayacak -->
            <androidx.gridlayout.widget.GridLayout
                android:id="@+id/grdLytOut"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginBottom="12dp"
                app:columnCount="3"
                app:rowCount="2">
            </androidx.gridlayout.widget.GridLayout>

        </ScrollView>

        <!-- Alt Buton Paneli -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:padding="8dp">

            <!-- Yeniden Başla Butonu -->
            <Button
                android:id="@+id/yenidenBaslaBtn"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="🔄 Yeniden Başla"
                android:textSize="16sp"
                android:textStyle="bold"
                android:backgroundTint="#FF9800"
                android:textColor="#FFFFFF"
                android:layout_marginEnd="8dp"
                android:paddingVertical="12dp" />

            <!-- Ana Menüye Dön Butonu -->
            <Button
                android:id="@+id/anaMenuBtn"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="🏠 Ana Menü"
                android:textSize="16sp"
                android:textStyle="bold"
                android:backgroundTint="#607D8B"
                android:textColor="#FFFFFF"
                android:layout_marginStart="8dp"
                android:paddingVertical="12dp" />
        </LinearLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
