<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <!-- Kamera izni (oyunda kullanılabilir) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    
    <!-- INTERNET izni: OkHttp ile HTTP istekleri için zorunlu -->
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- POST_NOTIFICATIONS izni: Android 13 (API 33) ve üzeri için bildirim göstermek için gerekli -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yeniproje"
        tools:targetApi="31">
        
        <!-- ==================== LAUNCHER ACTIVITY (İLK EKRAN) ==================== -->
        <!-- MenuActivity artık uygulama açıldığında ilk gösterilen ekrandır -->
        <activity
            android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- ==================== DİĞER ACTIVITY'LER ==================== -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".OyunActivity"
            android:exported="false" />
        <activity
            android:name=".AnaMenuActivity"
            android:exported="false" />
        <activity
            android:name=".AyarlarActivity"
            android:exported="false" />
        <activity
            android:name=".SkorActivity"
            android:exported="false" />
        <activity
            android:name=".SkorTableActivity"
            android:exported="false" />
        
        <!-- ==================== FIREBASE MESSAGING SERVICE ==================== -->
        <service
            android:name=".Bildirim"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>